services:
  - type: web
    name: lucky-snap-backend-simple
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node src/simple-backend.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
